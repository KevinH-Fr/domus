<%= form_with(model: home_sale) do |form| %>
  <% if home_sale.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(home_sale.errors.count, "error") %> prohibited this home_sale from being saved:</h2>

      <ul>
        <% home_sale.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= form.hidden_field :home_id, value: @home.id, class: "form-control" %>

  <div class="input-group input-group mt-4 mb-2">
    <%= form.label :price, "Prix", class: "input-group-text" %>
    <%= form.number_field :price, class: "form-control" %>
  </div>

  <div class="input-group input-group mb-2">
    <%= form.label :description, "Description", class: "input-group-text" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="mt-4">
    <%= custom_submit_button(form, "Créer l'offre de vente") %>
  </div>

<% end %>
