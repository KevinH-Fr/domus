<%= form_with(model: home) do |form| %>
  <% if home.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(home.errors.count, "error") %> prohibited this home from being saved:</h2>

      <ul>
        <% home.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="input-group input-group mb-2">
    <%= form.label :name, "Nom", class: "input-group-text" %>
    <%= form.text_field :name, class: "form-control" %>
  </div>

  <div class="input-group input-group mb-2">
    <%= form.label :address, "Adresse", class: "input-group-text" %>
    <%= form.text_field :address, class: "form-control" %>
  </div>

  <div class="input-group input-group mb-2">
    <%= form.label :city, "Ville", class: "input-group-text" %>
    <%= form.text_field :city, class: "form-control" %>
  </div>

  <div class="input-group input-group mb-2">
    <%= form.label :cp, "Code Postal", class: "input-group-text" %>
    <%= form.text_field :cp, class: "form-control" %>
  </div>

  <%= form.hidden_field :user_id, value: current_user.id, class: "form-control" %>

  <div class="mt-4">
    <%= custom_submit_button(form, "Créer le bien") %>
  </div>

<% end %>
