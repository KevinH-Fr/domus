<p style="color: green"><%= notice %></p>

<%= render @home %>


    <% if @home_sale.present? %>
      <div class="card p-2">
        <h3> Offre de vente </h3>
        <%= render @home_sale %>
      </div>
    <% else %>
      <%= link_to  "Proposer à la vente", new_home_sale_path(home: @home.id), class: "btn btn-success" %>
    <% end %>
    

<hr>

<%= link_to  "Proposer à la location", new_home_sale_path(home: @home.id), class: "btn btn-success" %>
 